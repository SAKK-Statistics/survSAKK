library(covr)

# Generate the coverage report
report <- covr::report()

# # Specify the directory where you want to save the report
# save_path <- "C:/Users/vithersans/Desktop"
#
# # Combine the save path with the report filename
# report_filename <- paste0(save_path, "coverage_report.html")
#
# # Save the report
# covr::report(report, file = report_filename)
